// import { useAuth } from '../hooks';

const Home = () => {
  // const auth = useAuth();
  return (
    <div>
      <h1>Welcome, {/* {auth.user.name} */}</h1>
    </div>
  );
};

export default Home;
